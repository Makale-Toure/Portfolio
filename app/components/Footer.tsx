import { Section } from "./Section"

export const Footer = () => {
    return <footer className="bg-card">
        <Section classname="py-8 items-center">
            <p className="text-muted-foreground text-sm">
                @Copywrite 2024 Makalé Touré
            </p>
        </Section>
    </footer>
}